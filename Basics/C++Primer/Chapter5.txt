表达式
表达式由一个或多个操作数通过操作符组合而成，每个表达式都会产生一个结果

复合表达式中操作数的分组结合取决于操作符的优先级和结合性

圆括号凌驾在优先级之上

求值顺序
&& ||：当且仅当其右操作数确实影响表达式值时，才计算这两个操作符的右操作数

f1() * f2()
由于没有规定求值顺序，所以不知道先调用f1()还是f2()

隐式转换

显式转换
c: ()
c++: static_cast, dynamic_cast, const_cast, reinterpret_cast
cast-name<type>(expression)