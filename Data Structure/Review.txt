数据结构
1)逻辑结构
    1)线性结构
        1)一般线性表
        2)受限线性表 - 栈 队列 串
        3)推广线性表 - 数组 广义表
    2)非线性结构
        1)树形结构 - 一般树 二叉树
        2)图状结构 - 有向图 无向图
2)物理结构
    1)顺序结构
    2)链式结构
    3)索引结构
    4)散列结构

//typedef int ElementType;

1)List

typedef struct{
    int capacity;
    int length;
    ElementType *data;
}SqList, *PtrSqList;

typedef struct LNode{
    ElementType data;
    struct LNode *next;
}LinkList, *PtrLinkList;

typedef struct DNode{
    ElementType data;
    struct DNode *proir;
    struct DNode *next;
}DoubleLinkList, *PtrDoubleLinkList;

2)Stack

typedef struct{
    int capacity;
    int top;
    ElementType *data;
}SqStack, *PtrSqStack;

typedef struct SNode{
    ElementType data;
    struct SNode *next;
}LinkStack, *PtrLinkStack;

3)Queue

typedef struct{
    int capacity;
    ElementType *data;
    int front, rear;
}SqQueue, *PtrSqQueue;

typedef struct QNode{
    ElemType data;
    struct QNode *next;
}QueueNode, *PtrQueueNode;

typedef struct{
    QueueNode *front;
    QueueNode *rear;
}LinkQueue, *PtrLinkQueue;

4)String

typedef struct{
    int capacity;
    char *data;
    int length;
}SqString, *PtrSqString;

typedef struct STNode{
    char data;
    struct STNode *next;
}LinkString, *PtrLinkString;

5)Tree
//双亲表示法
typedef struct{
    ElementType data;
    int parent;
}*ParentTree;
//孩子表示法
typedef struct TSNode{
    ElementType data;
    struct TSNode *sons[MAXSONS];
}SonTree, *PtrSonTree;
//孩子兄弟表示法
typedef struct TSBNode{
    ElementType data;
    struct TSBNode *son;
    struct TSBNode *brother;
}SonBrotherTree, *PtrSonBrotherTree;

6)Graph
//邻接矩阵

//邻接表

7)Cursor
typedef struct CNode{
    ElementType *data;
    int 
}*SqCursor;
